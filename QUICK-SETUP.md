SETUP SUMMARY:

✓ AWS: S3 bucket + Bedrock configured
✓ Auth0: M2M apps + API configured  
✓ Python: 3.11.14 + all deps installed
✓ .env: Fully configured (get from partner)

See partner's backend/.env for all credentials.
See docs/SETUP-CREDENTIALS.md (local, gitignored) for reference.

Run: python scripts/verify-setup.py

